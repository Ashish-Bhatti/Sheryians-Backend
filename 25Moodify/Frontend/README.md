Step 1 :-
            npm install @mediapipe/tasks-vision

Step 2 :- we are using black box code method in which we user an external function which we don't fully understand but we know what it's doing so we use that from our class

----following 4 step of react architecture-----
 UI => it display the UI and handle navigation, usually create - pages, components

 Hook => manage state and api layer , usually create - hook

 State => data store in states , usually create - authContext, postContext etc

 API => communicate with the backend , usually create = services/auth.api.js , sevices/post.auth.js